Retirar o código escrito dentro da função "main" e colocar em 
uma função chamada "exibir_mapa", o tipo da função será void e 
não receberá nenhum argumento.

    /*mapa[inimigox][inimigoy] = '.';

    int dx[] = {-1, 1, 0, 0};
    int dy[] = {0, 0, -1, 1};
    int menor_distancia = INFINITO, melhor_x = inimigox, melhor_y = inimigoy;

    for (int i = 0; i < 4; i++) {
        int nx = inimigox + dx[i];
        int ny = inimigoy + dy[i];

        if (nx >= 0 && nx < linhas && ny >= 0 && ny < colunas && mapa[nx][ny] != '#') {
            int distancia = heuristica(nx, ny, jogadorx, jogadory);
            if (distancia < menor_distancia) {
                menor_distancia = distancia;
                melhor_x = nx;
                melhor_y = ny;
            }
        }
    }

    inimigox = melhor_x;
    inimigoy = melhor_y;
    mapa[inimigox][inimigoy] = 'I';
    }*/